interface:
  display_name: "riida Release Prep"
  short_description: "Prepare and verify a riida release version update."
  default_prompt: "Cut the next riida release on a release/x.y.z branch: bump versions, update the changelog, regenerate bundled license notices, verify, merge the pull request once CI is green, tag the merge commit, and publish the GitHub Release."
