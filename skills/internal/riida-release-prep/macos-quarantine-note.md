> [!IMPORTANT]
> On macOS, `riida.app` may be blocked after installing it from the downloaded `*.dmg`.
> After copying it into `/Applications`, run:
>
> ```bash
> xattr -cr /Applications/riida.app
> ```
>
> Then open `riida.app` again.
